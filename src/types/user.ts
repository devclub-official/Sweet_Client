export interface UserInfo {
  bio: string;
  followersCount: number;
  followingCount: number;
  id: number;
  isFollowing: boolean;
  postsCount: number;
  profileImage: string;
  username: string;
}
